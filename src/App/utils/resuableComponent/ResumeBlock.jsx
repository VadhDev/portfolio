const ResumeBlock = ({title, location, timeline, arrayDescription}) => {
    return (
        <div>
            <h4>{title}</h4>
            <h5>{location}</h5>
            <h6>{timeline}</h6>
            <div>

            </div>
        </div>
    )
}

export default ResumeBlock;